keys:
  # --- Admin / Recovery Keys ---
  - &master       "age1jxg7esut03as8rc4yn2ql3zusfgshfg2vhhyem5tc3a9t0jlw4qqqlt5f5"

  # --- Machine Host Keys ---
  # Live USB
  - &nixos_btw    "age1xpzytgl37lrwtn4a67cja7jccjtwtu3gfzusdtp23hu54hdlrqaqlexk88"

  # - &desktop    "age1..."

creation_rules:
  # 1. Common secrets (Readable by all your machines + master)
  - path_regex: secrets/common\.yaml$
    key_groups:
      - age:
          - *master
          - *nixos_btw
          # - *desktop # Uncomment when added

  # 2. Host-specific secrets for 'nixos-btw'
  - path_regex: secrets/hosts/nixos-btw\.yaml$
    key_groups:
      - age:
          - *master
          - *nixos_btw

  # 3. Host-specific secrets for 'desktop' (Prepared for future)
  # - path_regex: secrets/hosts/desktop\.yaml$
  #   key_groups:
  #     - age:
  #         - *master
  #         - *desktop
